export default function HomePage() {
  return (
    <main className="page-shell">
      <section className="hero-card" aria-labelledby="home-title">
        <p className="eyebrow">Setup ready</p>
        <h1 id="home-title">Page Home Pay</h1>
        <p>
          This temporary landing page keeps the Next.js validation pipeline
          executable while the product layout, authentication, and category
          features are implemented in their own issues.
        </p>
      </section>
    </main>
  );
}
